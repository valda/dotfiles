# Claude Code Instructions

@AGENTS.md

Claude Code should treat `AGENTS.md` as the canonical project guide. Keep this
file as a thin import entrypoint and place shared repository instructions in
`AGENTS.md` to avoid drift between Codex and Claude Code.
