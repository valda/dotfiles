[color]
	branch = auto
	diff = auto
	status = auto

[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green

[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold

[color "status"]
	added = yellow
	changed = green
	untracked = cyan

[user]
	name = YAMAGUCHI Seiji
	email = valda@underscore.jp

[branch]
	autosetupmerge = true

[alias]
	st = status
	ci = commit
	br = branch
	co = checkout
	amend = commit --amend

[github]
	user = valda
	token = !pit get github 2>/dev/null | awk '/token:/ {print $2}'
[gui]
	recentrepo = C:/Fallout 3
